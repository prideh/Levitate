<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Levitate" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Levitate">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#050505] text-zinc-300 font-['Outfit'] antialiased">
    <div class="flex h-screen overflow-hidden">
      <!-- Sidebar / Recent Discoveries -->
      <aside class="w-64 bg-[#0a0a0a] border-r border-white/5 flex-shrink-0 hidden md:flex flex-col">
        <div class="p-6 border-b border-white/5">
          <%= link_to "Levitate", root_path, class: "text-2xl font-bold tracking-tight text-white hover:text-zinc-300 transition" %>
        </div>
        
        <div class="p-4 flex-1 overflow-y-auto">
          <h2 class="text-xs font-medium text-zinc-500 uppercase tracking-widest mb-4 px-2">Recent Discoveries</h2>
          <%= render "recommendations/recent_history" %>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 flex flex-col overflow-hidden relative">
        <!-- Top Bar (Mobile potentially) -->
        <header class="bg-[#0a0a0a] border-b border-white/5 p-4 md:hidden">
          <%= link_to "Levitate", root_path, class: "text-xl font-bold text-white hover:text-zinc-300 transition" %>
        </header>

        <!-- Content Scroll Area -->
        <div class="flex-1 overflow-y-auto p-4 md:p-8">
          <% flash.each do |key, message| %>
            <div class="mb-4 p-4 rounded-xl text-sm font-medium border <%= key == 'alert' ? 'bg-red-950/30 text-red-200 border-red-900/50' : 'bg-blue-950/30 text-blue-200 border-blue-900/50' %>">
              <%= message %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
